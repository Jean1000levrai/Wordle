

function HistoryContainer( {children} ){
    return(
        <div className="historyContainer">
            <p>
                {children}
            </p>
        </div>
    )
}

export default HistoryContainer